package org.example;

import java.util.ArrayList;
import java.util.List;

public class MyCollectionExample {


    //List form the Collection

    /**
     * List is the interface and implementing classes are ArrayList,LinkedList,Vector -> Stack (Subclass of vector)
     *
     * ArrayList --> maintain the order and dynamic, contains element of similar type ,
     *                The ArrayList class has a regular array inside it. When an element is added, it is placed into the array.
     *                If the array is not big enough, a new, larger array is created to replace the old one and the old one is removed.
     *                DEFAULT CAPACITY -> 10
     * LinkedList--->
     *
     *
     */


}